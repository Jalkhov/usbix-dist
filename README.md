# usbix-dist

Repositorio de distribucion de USBix: **solo artefactos** (manifest firmado y
reglas). El codigo fuente es privado.

- `updates.json` / `updates.json.sig` — manifest y firma Ed25519.
- `usbix-rules.tsv` — reglas de deteccion.
- Los instaladores van en **Releases**.
